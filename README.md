# homebrew-tap

Homebrew tap for jefrontv tools.

```sh
brew install jefrontv/tap/agent-local
```

Casks in this tap are generated by [GoReleaser](https://goreleaser.com) on each
release — edit the source project, not the files here.
